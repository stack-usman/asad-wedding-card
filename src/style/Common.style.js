import styled from "styled-components"

export const ButtonStyle = styled.button`
    background: transparent;
    color: #fff;
    border: 0;
    padding: 5px 15px;
    outline: none;

    &:focus {
        border: 0;
    }

    img {
        width: 65px;
    }
`